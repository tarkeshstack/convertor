<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Needed to enumerate installed apps on Android 11+ (API 30+) package visibility rules -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Optional: only requested at runtime, only used to resolve "call/message <name>" to a phone number -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Optional: only requested at runtime, only used for the mic button's voice input -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Only used by the in-app mini browser (command builder's "Browse for a link"),
         to load the pages you navigate to there -->
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <!-- Let us see every launchable app for search + open -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>

        <!-- Needed to reach the system speech-recognition service for voice input -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>

        <!-- Known integrations used for command execution -->
        <package android:name="com.ubercab" />
        <package android:name="com.olacabs.customer" />
        <package android:name="com.whatsapp" />
        <package android:name="com.google.android.apps.maps" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.spotify.music" />
        <package android:name="com.amazon.mShop.android.shopping" />
        <package android:name="com.google.android.gm" />

        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.SmartAppLauncher"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SmartAppLauncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Lets this app appear in other apps' Share sheet: pick "Smart Launcher"
                 when sharing a link out of another app to capture it as a deep link,
                 without needing to know that app's URI scheme. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>

</manifest>
