<?xml version="1.0" encoding="utf-8"?>
<!-- A diagonal gradient instead of a flat color, so the icon reads with some depth
     behind the phone glyph rather than a completely flat backdrop. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="135"
        android:startColor="#5A8CEB"
        android:endColor="#122A57" />
</shape>
